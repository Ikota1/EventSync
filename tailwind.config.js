/** @type {import('tailwindcss').Config} */
export default {
  content: [
    `./index.html`,
    `./src/**/*.{js,jsx}`
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}

// This has to be reviewed if its properly configured later